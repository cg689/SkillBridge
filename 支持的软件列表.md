# 支持的应用列表

本文件列出 SkillBridge 当前自动同步的全部目标应用。每个应用都会从 CC Switch 技能库（`~/.cc-switch/skills`）通过 junction / symlink 接到自己的技能目录。路径一律用环境变量，不写死用户名或盘符。

权威名单在 [`supported-tools.json`](./supported-tools.json)；`detect-tools.ps1` / `detect-tools.sh` 与 `config.example.json` 都从这里生成/对齐。

## 同步目标（共 23 个）

| # | 应用 | 技能文件夹 | 备注 |
|---|------|------------|------|
| 1 | Claude Code | `%USERPROFILE%\.claude\skills` | Cursor 兼容读取此目录 |
| 2 | Cursor | `%USERPROFILE%\.cursor\skills` | **copy 模式**（真实目录）。Cloud Agent 不跟随 symlink，也不能直接读本机 `~/.cc-switch`；需 Sync Skills 或 `--copy-into .cursor/skills` |
| 3 | Gemini CLI | `%USERPROFILE%\.gemini\skills` | |
| 4 | Cline | `%USERPROFILE%\.cline\skills` | 安装后由 detect-tools 自动纳入 |
| 5 | Kilo Code | `%USERPROFILE%\.kilo\skills` | 安装后由 detect-tools 自动纳入 |
| 6 | ZCode | `%USERPROFILE%\.zcode\skills` | |
| 7 | WorkBuddy | `%USERPROFILE%\.workbuddy\skills` | 国内版 |
| 8 | WorkBuddy AI | `%USERPROFILE%\.workbuddy-ai\skills` | 海外版 |
| 9 | Comate | `%USERPROFILE%\.comate\skills` | |
| 10 | Hermes Agent | `%HERMES_HOME%\skills` | 需设置 `HERMES_HOME`；扣子（Coze）经 hermes 后端也可读取 |
| 11 | TRAE Work CN | `%USERPROFILE%\.trae-cn\skills` | |
| 12 | Cherry Studio | `%APPDATA%\CherryStudio\Data\Skills` | |
| 13 | CodeBuddy CN | `%USERPROFILE%\.codebuddy\skills` | 与已有 dbs 技能合并去重 |
| 14 | Codex CLI / DeepSeek | `%USERPROFILE%\.agents\skills` | `.agents` 系；Cursor 也会扫描此用户级目录 |
| 15 | Codex | `%USERPROFILE%\.codex\skills` | CC Switch 的 Codex 目标；Cursor 兼容读取 |
| 16 | OpenCode | `%USERPROFILE%\.config\opencode\skills` | CC Switch 的 OpenCode 目标目录 |
| 17 | AutoClaw | `%USERPROFILE%\.openclaw-autoclaw\skills` | |
| 18 | Verdent | `%USERPROFILE%\.verdent\skills` | |
| 19 | Qoder CN | `%USERPROFILE%\.qoder-cn\skills` | |
| 20 | 豆包 Doubao | `%USERPROFILE%\DoubaoWork\skills` | |
| 21 | MiniMax Code | `%USERPROFILE%\.minimax\skills` | 路径为按惯例新建，建议在工具内核对 |
| 22 | 千问办公 Qwen Office | `%USERPROFILE%\.qwenworkcn\skills` | |
| 23 | Grok Bot | `%USERPROFILE%\.grok\skills` | 即 CC Switch 界面里的 Grok Build |

Unix 上 `%USERPROFILE%` 展开为 `$HOME`，`%APPDATA%` 展开为 `${XDG_CONFIG_HOME:-~/.config}`。

### 关于 Cursor

Cursor 会加载 `~/.cursor/skills/`（用户级）以及项目内的 `.cursor/skills/`。为兼容还读取 `~/.claude/skills/`、`~/.codex/skills/`、`~/.agents/skills/`。

SkillBridge 对 Cursor 使用 **copy**（不是 junction）：Cursor 本地发现和 Cloud Agent「Sync Skills」都不会展开符号链接。即便如此，云端虚拟机默认仍然没有你电脑上的文件，所以还要：

- 打开 Sync Skills，并从桌面 Agents 窗口启动；或
- 在具体仓库执行 `sync-skills --copy-into .cursor/skills`，让项目级 skill 出现在 checkout 里。

若 Claude / Codex / `.agents` 也在 `targets` 里，本地 Cursor 可能把同一 skill 发现多次——可从 `targets` 里去掉不需要的那几行。
